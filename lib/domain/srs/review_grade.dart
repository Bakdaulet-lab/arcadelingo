/// Оценка ответа — единственное, что планировщик знает о том, как человек ответил.
///
/// Игра сюда не смотрит и эту шкалу не выставляет: она отдаёт сырой
/// `ReviewOutcome`, а перевод факта в оценку живёт в domain/srs. Иначе каждая
/// новая игра изобретёт собственную шкалу и они разъедутся.
library;

/// Порядок значений — от худшего к лучшему: на него опирается правило
/// «hintsUsed > 0 понижает оценку на одну ступень».
enum ReviewGrade { again, hard, good, easy }
