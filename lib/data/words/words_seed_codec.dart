/// Разбор сида слов `assets/words_seed.json` в единицы показа.
///
/// Реализации здесь ещё нет — только сигнатура для тестов задачи 0.5.
library;

import 'package:arcadelingo/domain/core/result.dart';
import 'package:arcadelingo/domain/review/review_contract.dart';

Result<List<ReviewItem>> parseWordsSeed(String json) =>
    throw UnimplementedError('разбор сида не реализован — задача 0.5');
