# Контекст проекта

Живой файл. Сюда пишем то, что выяснили и не хотим выяснять заново в следующей
сессии. Контекст сессии смертен, этот файл — нет.

Начало новой сессии: «прочитай `docs/dev/*.md` и продолжи с задачи N».

## Принятые решения

| Дата | Решение | Почему | Пересмотреть, если |
|---|---|---|---|
| — | Лейтнер, а не SM-2/FSRS, в v1 | Полностью понятен и тестируем; игра даёт бинарный ответ + время, а не 4-кнопочную оценку | Появятся данные, что интервалы промахиваются |
| — | Первая игра — падающие слова | Аркадное ощущение на обычных виджетах, естественно даёт `responseTime` | Гейт-1 покажет, что механика не держит |
| — | Без Drift/Riverpod/freezed в неделю 1 | Церемония до проверки продуктовой гипотезы — потраченное время | Пройден Гейт-1 |
| — | TTS вместо записанного аудио | Десятки мегабайт бандла ради функции меньшинства | Понадобится качество озвучки |
| — | Состояние Лейтнера — один ключ `leitner_state`, JSON-документ v1 | В legacy-API prefs нет транзакций: ключ-на-слово даёт смешанное состояние при крэше между записями; одно чтение; есть где хранить `version`. 50 карточек ≈ 3 КБ | Фаза 2: миграция в Drift |
| — | `due` читается строго: строка без обозначения зоны — битые данные | `DateTime.parse` без зоны даёт локальное время устройства — момент зависит от того, где читают. Пишем только `Z`; явное смещение допустимо, момент однозначен | Появится внешний источник, пишущий без `Z` |
| — | Битый документ состояния — `Err` целиком, стор ничего не сбрасывает | Частичное восстановление и молчаливый сброс прячут проблему и уничтожают улики. Судьба `Err` — задача 0.6 | — |
| — | «Отсутствующий ключ» в 0.5 — два случая: нет ключа prefs → пусто (первый запуск); нет поля `box`/`due` в записи → `Failure` | Страх — подмена отсутствующего значения нулём, а не первый запуск | — |
| — | Legacy `SharedPreferences`, не `SharedPreferencesAsync` | In-memory фейк `setMockInitialValues` есть в самом пакете; async-API требует `shared_preferences_platform_interface` отдельной зависимостью | Фаза 2 (Drift) или deprecation legacy-API |
| — | `responseRatio()` публичная, рядом с `gradeOutcome()` в `lib/domain/srs/` | Правило «`responseTime > timeLimit` → ratio зажат в 1.0» через одну оценку ненаблюдаемо: 1.0 и 2.5 оба дают `hard`, тест на зажим без самого ratio подтверждал бы любую реализацию | Появится второй потребитель ratio (статистика, SM-2) — решать, где ему жить |
| — | Очередь сессии: готовые ≤ `target`, повтор коробки 1 на 4-ю позицию **вместо** последней не-повторной карточки, один повтор на слово, `total` постоянен | Рост `total` на повторах менял бы SPEC и док контракта и заставлял HUD ползти на верных-но-медленных ответах; вытеснение держит размер сессии и отдаёт слот закреплению вместо нового слова. Впереди < 4 — повтора нет: иначе «сразу после подсветки ответа» | Гейт-1: сессии однообразны → ручка в порогах `grade_outcome` или «`hard` на коробке 1 не повторяет», не в логике очереди |
| — | `hard` на коробке 1 повторяет в сессии: правило по карточке (`due <= now`), не по оценке | Так говорит таблица скилла; новичок на новых словах часто даёт `hard`, принято явно. При SM-2 ничего не бывает готово немедленно — правило исчезнет само | — |
| — | `Err` из `load()` и из сида хост разбирает по отдельности; автосброса нет, `reset()` — только кнопкой пользователя | Реакция разная: битое состояние — сброс по решению пользователя, битый сид — дефект сборки, сброс ничего не чинит. Склейка двух `Result` в один стёрла бы, что именно упало | — |
| — | Время в сессию — функцией `now`, вызов на каждый `report()`; `arch_check` правило 3 на весь `lib/domain` | Сессия может провисеть в фоне; `schedule` считает от ответа, а не от старта. Сессия — первый код domain вне srs, которому нужно время | — |
| — | `onCardsChanged` после каждого `report()` полной картой; хост — `unawaited(store.save(cards))`; `false` платформы теряется | `void report()` не даёт `await`; `save()` кодирует синхронно до первого `await` (закреплено тестом стора). При убийстве приложения теряется только элемент в полёте | Фаза 2 (Drift): видеть результат записи |
| — | Нарушение протокола `ReviewSession` — `StateError`; сессия владеет копией карты из `load()` | game-contract: ноль и два `report()` портят расписание молча, бросок делает их громкими в тестах игры. Копия — никакого алиасинга с хостом | — |
| — | Логика игры — чистый класс `FallingWordsRun` без Flutter, виджет над ним тонкий | Страж «ровно один `report()` на слово» обязан жить в состоянии, а не в обработчике тапа: второй тап того же кадра приходит в ещё не перестроенное дерево, и `IgnorePointer` его не останавливает. Заодно правила игры проверяются без дерева виджетов | — |
| — | Всё время игры — два `AnimationController` с `AnimationBehavior.preserve`; конец падения ловится по `value >= 1.0`, не по `AnimationStatus.completed` | `Timer`/`Stopwatch`/`DateTime.now()` не переживают `tester.pump` и паузу. `preserve` — потому что системное «убрать анимации» ускоряет обычный контроллер в 20 раз, а время падения здесь геймплей, а не украшение. Про статус — в «Тупиках» | — |
| — | Пауза на любом состоянии, кроме `resumed`, включая `inactive` | На `inactive` (шторка, звонок, переключатель приложений) кадры продолжают идти, и без паузы время начислялось бы. Оно же единственное состояние, на котором паузу вообще можно проверить тестом — см. «Тупики» | Гейт-1: пауза на `inactive` сбивает темп (например, после баннера уведомления) |
| — | Выход из игры с выданным словом (`dispose()` в фазе падения) → `report(correct: false, responseTime: elapsed)`. Брошенное слово = промах и уходит в коробку 1 | Продуктовое решение, а не техническое. game-contract третьего варианта не даёт: «либо доложи неответ, либо не бери item». Молчание порвало бы протокол — следующий `nextItem()` бросил бы `StateError` | Гейт-1: наказание за выход раздражает. Тогда — не считать выход промахом (просить у сессии право вернуть слово) либо не брать `nextItem()` до первого кадра падения |

## Что выяснили про кодовую базу

### Калибровка дистракторов (из задачи 0.2, `assets/words_seed.json`)

50 слов подобраны вручную — это эталон для генератора К3. Что вылезло по ходу:

- **Стратегия зависит от части речи.** Для прилагательных и глаголов рабочая формула —
  антоним + два семантических соседа (`open` → закрывать, ломать, толкать). Для
  существительных антонимов нет: там категория + близкая частотность (`apple` → груша,
  слива, вишня). Генератору К3 понадобятся **разные стратегии по частям речи**, а не
  одна общая функция с фильтром по `part_of_speech`
- **Зеркальные пары взаимно обманывают друг друга.** У `open` в дистракторах «закрывать»,
  у `close` — «открывать». На 50 словах это приемлемо, но генератору нужно правило
  против показа обоих слов пары в одной сессии: иначе игрок видит один и тот же набор
  кнопок дважды и запоминает позицию, а не слово
- **`good` — эталон слабого набора** (хороший · плохой, грустный, странный). Оценочные
  прилагательные хуже всего ложатся в семантические корзины: «грустный» про настроение,
  «странный» про оценку иного рода, общей шкалы нет. Когда будет чем мерить качество
  обманок — проверять на этом классе слов

### Кейс 8 Лейтнера даёт меньше, чем обещает названием (из ревью 0.4)

`test/domain/srs/leitner_test.dart`, «граница суток и смена зоны». Не переписан
намеренно — но и защитой считать его нельзя:

- Обе ветки сравнивают `good` из коробки 1, то есть `2 == 2` при любой
  реализации, не завязанной на календарный день. Календарную уже ловят кейсы
  1, 5 и 6 точным `due` — вот они и есть настоящая защита
- Вторая половина (UTC против `toLocal()`) проверяет семантику `DateTime`,
  а не `schedule()`: `toLocal()` сохраняет момент, `add` работает по epoch
- Ветка со сменой зоны **ненаблюдаема на CI**: там обычно UTC, и `toLocal()`
  становится тождественным. На машине разработки зона Qyzylorda (+5, без
  перехода на летнее время) — тоже не худший случай. Реальный DST-переход
  этим тестом не покрыт нигде

Формулировке скилла («не меняют номер коробки») ассерт соответствует и слабее
её не стал, поэтому в 0.4 он оставлен как есть. Если понадобится настоящая
проверка часовых поясов — нужен пакет с TZ-базой и фиксированная зона в тесте,
а не системная. Инвариант «`due` всегда в UTC» держится конструктором
`LeitnerCard` и покрыт отдельными тестами — это надёжнее, чем ловить зоны.

### Разбор JSON под strict-casts (из задачи 0.5)

- `as` и `.cast<T>()` на данных из `jsonDecode` кидают `TypeError`, а не
  `FormatException`: `on FormatException` его не поймает, и вместо `Failure`
  будет крэш. `.cast()` к тому же ленивый — вылетит при обращении к элементу,
  уже в игре. В `lib/data/` на недоверенных данных — только `is` и паттерны;
  в тестах на известной фикстуре `as` допустим
- `Ok(const {})` из стора — ловушка: сессия дописывает карточки в карту, и
  `const` упадёт с `UnsupportedError` только на первом запуске (после первого
  `save` карта приходит из кодека обычная). Отсюда правило «карта из `load()`
  всегда новая и изменяемая», закреплено тестом стора

## Открытые вопросы

- Лицензия списка слов под коммерческое использование — не проверена
- Формат поставки контента: ассет в бандле или загрузка при первом запуске
- Нужен ли Flame и под какую именно игру
- Аркада на времени принципиально не проходится со screen reader и с
  включённым «убрать анимации»: таймер обойти нечем, а `preserve` мы ставим
  намеренно. Задача Р6 в `tasks.md` закрывает минимум (объявления вердикта,
  подписи HUD, отключение таймаута при `accessibleNavigation`), но режим без
  тайминга — отдельное продуктовое решение. Не всплывать этому на Фазе 5
- PreToolUse-хук защищает от Edit/Write/MultiEdit по `*.g.dart` / `*.freezed.dart` /
  `*.mocks.dart`, но не от записи через Bash (`cat >`, `sed -i` и т.п.) — осознанно
  не закрыто, риск низкий. Пересмотреть, если увидим реальный случай обхода

## Тупики, в которые не надо возвращаться

### Время и пауза в аркадной игре (из задачи 0.7)

- `Ticker.muted` и `TickerMode` паузы **не дают**: часы под ними идут, колбэк
  молчит, и на размьюте слово телепортируется на столько, сколько прошло.
  Пауза — только `AnimationController.stop()`, возврат — `forward()` без
  `from`: он строит новую симуляцию от текущего `value` с той же скоростью,
  поэтому `value × duration` не видит времени в фоне
- `lastElapsedDuration` не годится для «сколько прожило слово»: обнуляется на
  каждом `forward()` и после паузы показывает только последний отрезок,
  а после `stop()` вообще `null`. Считать надо от `value`
- `AnimationStatus.completed` приходит **кадром позже**, чем `value` достигает
  1.0: `_InterpolationSimulation.isDone` — строгое `t > duration`. На статусе
  не проверить ни «ровно 800 мс подсветки», ни таймаут в том же кадре;
  завершение ловится по значению, с обязательной проверкой фазы
- Тест паузы на `AppLifecycleState.paused` **ничего не доказывает**: на этом
  состоянии фреймворк сам выключает кадры (`_setFramesEnabledState`), тикер не
  тикает независимо от игры, и реализация, которая не паузит вовсе, проходит
  такой тест. Найдено мутационной проверкой уже написанных тестов. Проверять
  на `inactive`, где кадры идут; `paused` оставить для сценария «свернул и
  вернулся»

### Детерминированное время в widget-тестах (оттуда же)

- Анимация, запущенная **вне кадра** (обработчик тапа, сообщение lifecycle),
  ставит `Ticker._startTime` только на следующем тике: после такого действия
  нужен пустой `tester.pump()`, иначе все последующие `pump(Δ)` смещены на
  кадр. Запуск из слушателя другого контроллера взводится сразу — он уже
  внутри кадра
- `pumpAndSettle` в игре с таймером бесполезен: он домотает падение до
  таймаута. Время только явными `pump(Δ)`
