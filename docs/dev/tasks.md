# Задачи

Одна задача = один цикл Explore → Plan → Code → Verify → Commit = один PR.
Статусы: `[ ]` не начато, `[~]` в работе, `[x]` готово с зелёным `verify.sh`.

## Гейт-0. Walking skeleton (неделя 1)

- [x] **0.1** `flutter create`, комплект стартовых файлов на месте, `./scripts/verify.sh`
      отрабатывает (тесты пока пустые — это ожидаемо на этом шаге)
- [x] **0.2** `assets/words_seed.json` — 50 слов: `text`, `translation`,
      `part_of_speech`, три дистрактора руками. Именно руками: это калибровка того,
      какими обманки вообще должны быть
- [x] **0.3** Тесты Лейтнера по кейсам 1–8 из `.claude/skills/srs-engine/SKILL.md`.
      Падающие. Коммит тестов отдельно
- [x] **0.4** Реализация Лейтнера, тесты зелёные. Тесты не трогать
- [x] **0.5** Хранение состояния коробок в `shared_preferences`; загрузка слов из ассета.
      Десериализация возвращает `Failure` на битых данных и НЕ доводит их до
      конструктора `LeitnerCard` — его `ArgumentError` сторожит инвариант, а не
      разбирает хранилище. Обязательные кейсы (падающие, до реализации):
      отсутствующий ключ; невалидный JSON; `box` вне 1..5; `due` не парсится
- [x] **0.5.1** Маппинг `ReviewOutcome` → `ReviewGrade` в `lib/domain/srs/` —
      Шаг 1 из `.claude/skills/srs-engine/SKILL.md`. Отдельной задачей, потому
      что пока маппинга нет, `ReviewGrade` может прийти в движок только из игры,
      а это ровно то, что запрещает `game-contract`: игра сообщает сырой факт,
      шкалу заводит domain. Номер дробный, чтобы не сдвигать 0.6–0.10, на
      которые уже есть ссылки в коде и коммитах.
      Обязательные тест-кейсы (падающие, до реализации):
      `timeLimit == Duration.zero` → `hard` (делить нельзя);
      `responseTime > timeLimit` → `ratio` зажат в 1.0, не больше;
      границы `ratio`: 0.30 (`easy`/`good`) и 0.60 (`good`/`hard`) — обе стороны
      каждой границы, включая точное попадание;
      `hintsUsed > 0` понижает на ступень, но не ниже `hard`;
      `correct == true` никогда не даёт `again`
- [x] **0.6** Реализация `ReviewSession` поверх Лейтнера (простая, без репозиториев).
      Обязательный тест: карточка из коробки 1 всплывает не раньше чем через
      3 другие. Правило переехало сюда из Лейтнера в 0.4 и сейчас не покрыто
      нигде — это единственная дыра, оставленная сознательно.
      Решить судьбу `Err` из `LeitnerPrefsStore.load()` — сброс состояния или
      экран ошибки: стор на битом документе сам ничего не сбрасывает (0.5),
      выбор за этим слоем и должен быть явным
- [x] **0.7** Игра «падающие слова» по `SPEC.md`: сначала widget-тесты из раздела
      «Проверка», потом реализация.
      Игра обязана строить `ReviewOutcome` с неотрицательными `responseTime`
      и `hintsUsed` — сторожится на стороне игры, domain это не проверяет
      (сказано в шапке `lib/domain/srs/grade_outcome.dart`)
- [x] **0.8** Экран итогов и экран «на сегодня всё».
      Из ревью 0.7: заголовок «Раунд окончен» одинаков и когда сессия
      пройдена, и когда кончились жизни (`lives == 0` уже есть); кнопок нет
      вовсе — уйти можно только системным «назад»; строки «возвращайся
      завтра» у итогов нет, хотя у «на сегодня всё» она есть; «Верных
      ответов: 3 из 3» при HUD «1/15» выглядит багом, пока не объяснено,
      что слов к повторению было три.
      Экран ошибки состояния (`store.load()` → `Err`): текст `Failure` и явная
      кнопка «Сбросить прогресс» → `LeitnerPrefsStore.reset()` → повторный старт;
      автосброса в коде нет, решает пользователь (0.6). Экран ошибки сида
      (`loadWordsSeed()` → `Err`) — без кнопки: дефект сборки, сбрасывать нечего.
      Два `Result` разбираются по отдельности. Сессию создавать по тапу «играть»
      (`now()` очереди иначе протухает): `LeitnerReviewSession.start(target: 15,
      now: DateTime.now, onCardsChanged: (cards) => unawaited(store.save(cards)))`.
      Сделано: строка «что дальше» на итогах — не безусловное «возвращайся
      завтра», а одна из трёх, и считает её хост: буквальная формулировка
      обещала бы неправду после раунда из 15 при 50 словах в сиде. Правило и
      формулировки — `docs/dev/context.md`, пересмотр после Гейта-1
- [x] **0.9** Голдены: восемь кадров, `test/golden/`. Все три запроса
      ревьюера 0.7 закрыты: кадр успеха снят в варианте near-miss (он
      надмножество обычного — тот же полёт очков плюс метка ×1.5),
      `NothingTodayView` и слово у самого низа на 99% пути. Кадр при
      шрифте 2× не снят намеренно: там открыта **Р7**, и эталон закрепил
      бы переполненное поле как правильную вёрстку. Debug-баннер отключён
      и в голденах, и в приложении, на второе есть тест.
      Эталоны принимает человек: `update()` компаратора бросает
      исключение, то есть `--update-goldens` не работает ни у кого.
      Процедура — `docs/dev/goldens.md`.
      **Устарело с 0.13:** кадры снимались и принимались локально на
      Windows. Теперь платформа эталонов — Linux, локальный прогон кадров
      выключен по умолчанию, кандидаты приезжают артефактом прогона CI.
      Найдено картинкой, чего не показывали числа: подкрашенное поле на
      серии 8 — резкий прямоугольник с жёсткой границей о HUD и кнопки;
      слово на 99% пути стоит к верхней кнопке ближе, чем на 12 dp.
      Оба — вопросы к Гейту-1, `docs/dev/context.md`
- [x] **0.10** Сборка `--release`, установка на телефон, полный круг сыгран вручную
- [x] **0.11** Джус: хаптика, тряска на промахе, тон фона от комбо, прилёт
      очков, бонус «в последний момент». Числа — в `SPEC.md`, раздел «Джус»,
      он написан до кода. Закрывает **Р4** целиком и **Р1** частично
      (успех получает свой отклик; галочка у верного слова и увод вверх
      остаются за Р1). Частицы и босс-слова — вне скоупа, записаны в
      «Вне скоупа» SPEC, чтобы не дорисовались «заодно».
      Решения этой задачи:
      бонус не трогает `ReviewOutcome` — это арифметика очков в оболочке,
      в `report()` уходит та же честная `responseTime`;
      порог и начисление целочисленные (`× 20 >= × 17`, `× 3 ~/ 2`) — на
      границе порога float дал бы тест, зелёный на одной машине и красный
      на другой;
      пара «слово → перевод» не трясётся вовсе, трясутся HUD и кнопки;
      джус не заводит ни одного нового `AnimationController` — тряска,
      прилёт и пульс читают `_reveal.value`, поэтому пауза работает даром,
      а выключение по `MediaQuery.disableAnimations` — умножение на ноль в
      `build`, а не подмена скорости часов;
      хаптика флагу `disableAnimations` не подчиняется: вибрация не
      движение на экране.
      Красный коммит обязан компилироваться: сигнатуры и константы входят
      в него с телами `UnimplementedError`, иначе `flutter test` не дойдёт
      до ассертов и `git diff <red> -- test/` перестанет быть сверкой
- [x] **0.12** Визуальная тема: тёмная палитра «ночной автомат», гарнитура
      Rubik (OFL, кириллица Cyreal), градиентный комбо-тон, стиль кнопок и
      HUD, множитель, загорающийся вместе с полем. Скоуп — только
      внешность: ни раскладка, ни хит-зоны, ни механика, ни тайминги не
      тронуты, и это проверено, а не обещано — 272 теста зелёные, включая
      оба на системный шрифт 2×, цели нажатия и контраст.
      Мутации на момент задачи: 68 из 68 (`theme` 12, `goldens` 11,
      `juice` 16, `game_juice` 22, `run_near_miss` 7), старые перепрогнаны
      на тёмной палитре. Наборов с тех пор прибавилось — число историческое,
      текущее печатает прогон.
      Что выяснилось по ходу и записано в `context.md`:
      жалоба «серо» была не про зерно, а про то, что `fromSeed` пастелит
      контейнеры при любом зерне;
      `fontWeight` на вариативном Rubik инертен — ось `wght` идёт только
      через `fontVariations`, иначе весь текст рисуется Light 300;
      файл шрифта переименован, потому что `FontManifest.json` хранит путь
      percent-encoded, а ключ ассета — сырой;
      у правила «тон гаснет мгновенно» отпало исходное основание: на
      тёмной палитре пара даёт 5.7 вместо 3.6. Правило оставлено, решение
      о поведении — отдельное.
      Порог разогрева один на поле и на множитель по коду, а не по
      договорённости: `comboIsHot()` спрашивают оба, и мутация
      «развязать пороги» краснеет.
- [x] **0.14** Экран «Источники»: содержимое `assets/ATTRIBUTION.md` в
      продукте. Условие лицензии CEFR-J — «cite properly» — адресовано
      пользователю, а репозитория он не видит, так что выполняться оно
      обязано на экране, а не только в файле.
      Файл остаётся единственным источником: второй копии текста в
      Dart-коде нет нигде, и ровно поэтому экран разбирает markdown сам, а
      не показывает свой текст через `showLicensePage`. На штатный экран
      лицензий ведёт кнопка — цитирование и юридическое приложение это
      разные вещи, и складывать их в один список значит прятать первое.
      Разбор частичный и знает об этом: незнакомая разметка уходит в
      `unsupported`, а тест разбирает живой файл и требует ноль таких
      блоков. Таблица в `ATTRIBUTION.md` через полгода даст красный тест в
      день внесения, а не мусор на экране в проде.
      Вход — домашний экран: на итогах кнопка конкурировала бы с «Ещё раз»
      и «Выйти». Из восьми голденов не задет ни один.
      Мутации: 11 из 11. Двенадцатая оказалась театром — явное срезание
      CRLF дублировало `trim()`, и код убран, а не оставлен страховкой.
      Две правки по ходу, обе не косметические: чтение ассета в
      widget-тесте — настоящий ввод-вывод, `pump()` его не дожидается
      (шов `DefaultAssetBundle` плюс отдельный тест на регистрацию в
      `pubspec`, который этот шов перестал ловить); `ListView` заменён
      колонкой, потому что `SelectionArea` над ленивым списком выделяет
      только построенное, а цитату обязаны копировать целиком

### Находки ревью 0.7, меняющие механику — решение за автором

Не сделаны в 0.7 сознательно: SPEC их не описывает, а «всё, что здесь не
описано, — вне скоупа». Первые две — про то, вернётся ли человек на восьмой
день, то есть их лучше закрыть до Гейта-1, иначе `gate1.md` ответит не на
тот вопрос. Остальные дешёвые.

- [~] **Р1** Успех звучит тише провала. Частично закрыт 0.11: прилёт очков,
      пульс счёта, тон фона от серии и хаптика. Осталось: галочка и
      `primary` у верного слова симметрично ошибке, увод слова вверх со
      scale вместо растворения в ноль. Промах: красное слово с крестом,
      красная кнопка, погасшее сердце, 800 мс. Верный ответ: слово тает в
      прозрачность за 300 мс, ничем не помечено, счёт меняется без анимации.
      Предложено: галочка и `primary` у верного слова симметрично ошибке;
      увести слово вверх со scale вместо растворения в ноль; пульс на счёте
      в момент прироста
- [x] **Р2** При раннем промахе верный перевод показан в 71% высоты экрана
      от слова — единственный момент, когда человек учится, и на саккаду
      через весь экран даётся 800 мс. Сделано до 0.8: на время подсветки
      промаха и таймаута поле заменяется парой «слово → перевод» по центру,
      кнопки гаснут, нажатая ошибочная остаётся помеченной. SPEC обновлён
- [ ] **Р3** У падения нет пола: слово дрейфует в пустоте, понять «осталось
      полсекунды или две» нельзя. Линия внизу поля, 2–3 dp
- [x] **Р4** Нет тактильного отклика: `HapticFeedback` на верный и неверный.
      Сделано в 0.11: три различимых рукой отклика, таблица в `SPEC.md`.
      Единственный канал, работающий с выключенным звуком, и второй канал
      для дальтоников. Звук в SPEC вне скоупа, вибрация — отдельный вопрос
- [ ] **Р5** Первое слово начинает падать в нулевом кадре, до того как
      человек нашёл кнопки; в 0.8 добавится ещё и переход экрана. Взвод
      600–800 мс перед первым падением
- [ ] **Р6** Со screen reader играть нельзя: слово не `liveRegion`, вердикт
      в семантику не уходит, HUD читается как «один слэш пятнадцать».
      Обойти таймер тоже нечем. Минимум: `liveRegion` и `announce` на
      вердикт, подписи HUD, отключение таймаута при `accessibleNavigation`
- [ ] **Р7** Игровое поле переполняется при системном шрифте 2× на экране
      360×640 dp (бюджетный 16:9): `RenderFlex overflowed by 92 pixels`,
      колонка `_playfield()`. Найдено в 0.8 — тест итогов на коротком
      экране сначала ловил именно поле, а не итоги. Существующий тест
      «системный шрифт крупнее» гоняется на 360×780, где ещё влезает.
      Итоги в 0.8 починены прокруткой; полю нужна своя раскладка, потому
      что прокручивать падающее слово нельзя

## Гейт на чужой машине (CI)

Не «неделя 1»: разворачивался после 0.12, когда стало что охранять.

- [x] **0.13** GitHub Actions: тот же `./scripts/verify.sh`, что зовут руками.
      Workflow не заводит второго определения «готово» — второе, живущее в
      YAML, разъедется с первым в тот же месяц, и узнают об этом по зелёному
      CI на сломанной ветке.
      Разворачивался тремя фазами, каждая своим коммитом:
      - [x] **фаза 1** — гейт делится по платформам ещё до появления CI: тег
            `golden` у восьми кадров, корневой `dart_test.yaml`, переменная
            `WORDARCADE_GOLDENS` (умолчание `off`) и отдельный
            `scripts/goldens.sh`. Отдельным скриптом, а не третьим режимом
            `verify.sh`, потому что строка «VERIFY: PASS» обязана значить одно
            и то же везде: прогон без analyze и формата права её печатать не
            имеет. Две канарейки шрифтов тегом НЕ помечены намеренно — ответ
            «Rubik не приехал» нужен на чужой машине сильнее, чем на своей
      - [x] **фаза 2** — сам workflow: `ubuntu-latest`, Flutter 3.29.2 пином,
            кэш pub, actions пинятся на commit SHA (теги `v2` подвижны, SHA —
            нет). Шаг с кадрами стоял под `continue-on-error`: эталоны были
            ещё windows-овые и краснели заведомо, а заведомо красный шаг за
            неделю приучает не смотреть на красное
      - [x] **фаза 3** — эталоны пересняты на Linux и приняты человеком по
            артефакту прогона #1; шаг с кадрами стал блокирующим,
            `WORDARCADE_GOLDENS` по умолчанию `off`. Выбран вариант (б) из
            трёх, открытых в 0.9. Измерено, а не выведено: одни и те же
            восемь кадров дают 8 из 8 расхождений на второй ОС в обе
            стороны. Цена названа честно — локально кадры не увидеть, они
            приезжают артефактом. Разбор `docs/dev/goldens.md`

## Трек К. Контент (параллельно, код не блокирует)

Нумерация из `ROADMAP.md`. Детали пайплайна — `.claude/skills/content-pipeline/SKILL.md`,
источники и лицензии — `docs/dev/content_sources.md`.

- [x] **К1** Список слов и лицензия под коммерческое использование, схема `level`.
      Источник — CEFR-J Wordlist 1.5 (не NGSL: у того условия не указаны).
      Уровни 50 слов сида не проставлены руками, а сверены с источником: все
      нашлись, расхождений по части речи ноль, 49 × `a1` и одно `a2` (`slowly`).
      Цена решения — цитирование обязательно с этого же коммита:
      `assets/ATTRIBUTION.md` едет в бандле, потому что «cite properly» адресован
      пользователю, а репозитория он не видит
- [x] **К2** Пайплайн переводов: импорт → порции по 50 → ручная вычитка →
      сведение в ассет. Инструмент готов целиком; вычитка порций идёт своим
      темпом и вынесена в К2.1, потому что это не «доделать пайплайн», а
      вечера за словарём.
      Инвариант: в `assets/` попадает только вычитанное, импортёр туда не пишет
      никогда (сторожится `assertOutDirAllowed`, а не комментарием)
      - [x] импортёр `tool/import_cefrj.dart`. Прогон на живом CSV: 7799 строк →
            A1+A2 2575 → четыре части речи 2330 → `^[a-z]+$` 2195 → схлопывание
            2012 → минус наши 50 = **1962 слова, 40 порций** (1–17 A1, 18–40 A2).
            Отсев 380 в `skipped.csv` с причиной, ничьи 85 в `ambiguous.csv`.
            CSV в репозитории нет, путь к нему — аргумент команды
      - [x] `tool/merge_portion.dart`: принятое дописывается в ассет (только
            дописывается — в диффе порции ровно +N строк плюс запятая),
            отклонённое → `tool/out/rejected.json` с причиной и датой, откуда
            импортёр исключает эти id наравне с сидом. Полузаполненная запись
            роняет сведение: пустая часть речи, пустой перевод, обманок не три.
            Правила содержания живут одним файлом `tool/seed_rules.dart` —
            их же применяет тест ассета
      - [x] порции 1–3: **+134 слова, сид 50 → 184**, отклонено 16 с причинами
            в `tool/out/rejected.json`, 26 ловушек в `tool/confusables.csv`.
            Стоп-правило сработало: сид из 50 кончался за пару сессий, эти три
            порции проблему сняли
      - [ ] остальные 37 порций — параллельно, код не блокируют. **Трек К уходит
            в фон**: следующая порция берётся тогда, когда 184 слов начнёт не
            хватать, а не подряд
- [~] **К2.1** Вычитка и сведение порций.
      - [x] порция 1 — 46 слов A1, сид 50 → 96. Из вычитки вышли четыре
            правила отбора, они в `docs/dev/context.md`
      - [ ] **порции 2–3** (+~100 слов) — следующие. Стоп-правило: сид кончается
            за пару сессий, эти две снимают реальную проблему
      - [ ] остальные 37 порций — параллельно, код не блокируют
- [ ] **К3** Генератор дистракторов. Правило №2 из скилла (±20% по рангу
      частотности) переписать под `level`: ранга у CEFR-J нет вовсе. Бьёт по
      существительным — у них стратегия «категория + близкая частотность»,
      и они же 62% импорта. Валидатор из К2 к этому моменту уже считает
      зеркальные пары и коллизии переводов
- [ ] **К4** Аудио: TTS на устройстве
- [ ] **К5** Формат поставки: SQLite-ассет в бандле против загрузки при первом запуске

## Бэклог (после Гейта-1)

- [ ] **Б1** Сессия не показывает оба слова пары из `tool/confusables.csv`
      в один день. Источник — файл, а не только автонаходки валидатора.
      Зеркальные пары (`open` ↔ `close`) валидатор находит сам по взаимности
      обманок, и с порции 2 сведение **отказывается** писать ассет, пока
      найденная пара не внесена в файл. Но почти-синонимы на разных карточках
      (`step` «шагать» / `walk` «ходить»), однокоренные переводы (`come`
      «приходить» / `walk` «ходить») и near-miss на чтении (`clock` «часы» /
      `hour` «час») механически не видны вообще — их вносит вычитывающий.
      Лечится в очереди сессии, а не в контенте: запрещать такие пары в сиде
      нельзя, они там намеренны

## Гейт-1 (день 8)

- [x] **1.1** Три дня подряд играл по своей воле (2 дня из 3, досрочно)
- [x] **1.2** Ответы на три вопроса записаны в `docs/dev/gate1.md`
- [x] **1.3** Решение зафиксировано: идём в Фазу 2 / меняем механику / меняем аудиторию

## Фаза 2 и далее

Раскрывается после Гейта-1. Не расписывай заранее — вероятность, что план
изменится после первой недели игры, высокая, и заранее написанные задачи
создадут ложное ощущение обязательства.

Ориентиры — `ROADMAP.md`.
